# 環境監査レポート — 2026-08-01

- 実行: named Workflow `env-audit`（args: `{"date": "2026-08-01"}`、run `wf_f5247d1b-00c`）
- 構成: 4観点の並列スキャン（表面積 / 重複 / 残置物 / ドリフト）、全エージェント読み取り専用
- 結果: **所見7件**（critical 0 / high 0 / medium 2 / low 5）
- 消費: subagent 約228k tokens・20 tool calls・約58秒・エラー0
- 観点別内訳: 表面積2件・重複2件・残置物3件・**ドリフト0件**（担当エージェントは `findings: []` で正常終了。エラーではなく「点検の結果、差分なし」の判定）
- 方針: 読み取り専用。本監査ではファイルの削除・変更を一切行っていない（recommendation は提案のみ）
- 前回比: [2026-07-06分](env-audit-2026-07-06.md)は所見12件（critical 1 / high 3 / medium 3 / low 5）。今回は critical・high 該当なし。前回 critical だった `settings.json` の git 未追跡ドリフトは今回検出されておらず、`make settings-pull` 等による解消が示唆される（本監査は差分の再検証までは行っていないため確定情報ではない）

## サマリー

| ID | severity | 要点 |
|---|---|---|
| SF-1 | medium | ecc プラグイン単体で常駐スキル数の大半（261件）を占める |
| DUP-01 | medium | ブラウザ自動化 MCP（chrome-devtools 系）が `chrome-cdp` と ecc 内蔵版の2系統で同時稼働 |
| SF-2 | low | installed 30プラグイン中 enabled は18件（installed と enabled の乖離。設計どおりで是正不要） |
| DUP-02 | low | コード探索・シンボル操作の役割が serena と言語別 *-lsp プラグインに分散 |
| CRUFT-1 | low | plugins/cache に temp_git_* / temp_subdir_*.clone の残置5個（計約480KB） |
| CRUFT-2 | low | skills/learned が空ディレクトリのまま残置（SKILL.md なし） |
| CRUFT-3 | low | ~/.claude/plugins/cache 合計874MB（temp_* の寄与は僅少、規模記録のみ） |

## Medium（2件）

### SF-1 — ecc プラグイン単体で常駐スキル数の大半（261件）を占める
- **evidence**: `/Users/kkmclab/.claude/plugins/cache/ecc/ecc/2.0.0/skills` 配下に distinct SKILL.md が261件（`find ... -maxdepth 2 -iname SKILL.md | wc -l` → 261、ディレクトリ数262と対応、`.cursor`/`.opencode` 等の言語ミラーは除外済み）。`settings.json` の `enabledPlugins` で `ecc@ecc` は `true`。個人スキル `dotdir/.claude/skills/*/SKILL.md` は26件（description合計約12,965文字）。有効プラグイン由来スキル概算合計は 261(ecc) + 4(academic-research-skills) + 3(deploy-on-aws) + 7(aws-serverless) + 2(toolcall-recover) + 28(superpowers) + 1(frontend-design) + 1(skill-creator) + 1(claude-code-setup) + 3(codex) = 311件で、個人スキル26件の約12倍。
- **recommendation**: ecc の中から実際に使う機能（言語別 review/build/test 等）を厳選し、常駐 `enabledPlugins` を分割・絞り込みするか、頻度の低いドメイン特化スキル（medical/homelab/trading等）群を無効化して description 予算消費を抑える。

### DUP-01 — ブラウザ自動化 MCP が2系統同時稼働
- **evidence**: `~/.claude.json` の `mcpServers` に `chrome-cdp`（command: `npx -y chrome-devtools-mcp@latest --browser-url=http://127.0.0.1:9222`）が登録済み。加えて `settings.json` の `enabledPlugins` で `ecc@ecc: true` が有効であり、ecc プラグイン内蔵の chrome-devtools MCP（`mcp__plugin_ecc_chrome-devtools__*`、click/navigate_page/take_screenshot/evaluate_script 等29ツール）も同時に稼働している。両者は同一パッケージ `chrome-devtools-mcp` 由来でツール名・機能がほぼ一致（`mcp__chrome-cdp__*` と `mcp__plugin_ecc_chrome-devtools__*` で29ツール名が同一）。なお `chrome-devtools-mcp@claude-plugins-official` プラグイン自体は `enabledPlugins` で `false`（無効）。
- **recommendation**: （事実列挙のみ。採否判断はしない）

## Low（5件）

### SF-2 — installed 30プラグイン中 enabled は18件
- **evidence**: `/Users/kkmclab/.claude/settings.json` の `enabledPlugins` は30エントリ中 `true` が18件（commit-commands, swift-lsp, frontend-design, superpowers, skill-creator, codex, ecc, linear, typescript-lsp, security-guidance, claude-code-setup, pyright-lsp, serena, explanatory-output-style, academic-research-skills, deploy-on-aws, aws-serverless, toolcall-recover）。`installed_plugins.json` には code-simplifier, context7, playwright, code-review, feature-dev, ralph-loop, example-skills, hookify, claude-md-management, octo, chrome-devtools-mcp, data-engineering の計12件がキャッシュのみで無効化済み。
- **recommendation**: 設計どおりの区別（installed≠enabled）であり是正不要。常駐カウント報告時は enabled=18 を使うこと。

### DUP-02 — コード探索・シンボル操作の役割が複数プラグインに分散
- **evidence**: `enabledPlugins` で `serena@claude-plugins-official: true`（find_symbol/find_declaration/find_referencing_symbols/get_diagnostics_for_file 等26ツール、LSPベースのシンボル探索・編集を提供）と、`typescript-lsp@claude-plugins-official: true` / `pyright-lsp@claude-plugins-official: true` / `swift-lsp@claude-plugins-official: true`（いずれもデフォード `LSP` ツールを共有登録、言語別 LSP 経由のコード解析を提供）が同時に有効。serena と各 *-lsp プラグインは「コード内シンボル解析・診断取得」という役割で機能が重複しうる。
- **recommendation**: （事実列挙のみ。採否判断はしない）

### CRUFT-1 — plugins/cache に残置 temp_git_* / temp_subdir_*.clone が5個
- **evidence**: `/Users/kkmclab/.claude/plugins/cache/` 直下: `temp_git_1784699470067_kezyp7`, `temp_git_1784699470068_n1p7eq`, `temp_git_1784699470068_wxvx1u`, `temp_subdir_1784699470066_7znwfe.clone`, `temp_subdir_1784699470068_yt2fks.clone`（いずれも2026-07-22 14:51作成、各96KB、計約480KB）。パターンから orca/plugin marketplace 更新処理の一時clone失敗残骸と推定される。
- **recommendation**: 該当5ディレクトリの中身を確認後、不要なら削除（`rm -rf`）。同時刻に生成されているため一括の更新失敗が原因と見られ、再発時は orca 側の一時ディレクトリクリーンアップ処理を確認。

### CRUFT-2 — skills/learned が空ディレクトリで SKILL.md が存在しない
- **evidence**: `/Users/kkmclab/dotfiles/dotdir/.claude/skills/learned/` はエントリ0件（2026-07-06 20:32作成のまま更新なし）。SKILL.md を持たない唯一のディレクトリ。
- **recommendation**: 用途不明な空ディレクトリのため、使われていなければ削除。継続学習用の予約領域であれば README や `.gitkeep` で意図を明示。

### CRUFT-3 — ~/.claude/plugins/cache 合計サイズ874MB
- **evidence**: `du -sh ~/.claude/plugins/cache` = 874M（`temp_*` の寄与は約480KBのみで大部分は正規キャッシュ）。
- **recommendation**: 削除ではなく規模の可視化目的。定期的なキャッシュ肥大化監視の参考値として記録に留める。
