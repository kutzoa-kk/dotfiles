name: xgboost
type: xgboost
params:
  objective: reg:squarederror
  eval_metric: rmse
  n_estimators: 100
  learning_rate: 0.1
  max_depth: 6
  min_child_weight: 5
  subsample: 0.8
  colsample_bytree: 0.8
  random_state: ${seed}
  verbosity: 0
